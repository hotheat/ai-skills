interface:
  display_name: "To Issues"
  short_description: "Split plans into tracer-bullet issue slices"
  default_prompt: "Use $to-issues to break this plan into independently grabbable tracer-bullet issues."
